VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon public key>
# "on" enables Pay by card via the Stripe edge functions
VITE_CARD_PAYMENTS=off

# Public address of the shop, no trailing slash. Used for the canonical link, the
# link-preview image and sitemap.xml at build time.
VITE_SITE_URL=https://casadelvino.shop
# Optional: a Google Analytics 4 measurement id (G-XXXXXXXXXX) to send page views there too.
VITE_GA_ID=

# Cloudinary: cloud name + an UNSIGNED upload preset (Settings → Upload → Upload presets)
VITE_CLOUDINARY_CLOUD_NAME=<cloud name>
VITE_CLOUDINARY_UPLOAD_PRESET=<unsigned preset name>
